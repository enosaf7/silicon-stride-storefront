import React from 'react';

// This component is no longer needed since we simplified the chat system
// Keeping it empty to avoid breaking imports
const UserMessages = () => {
  return null;
};

export default UserMessages;
